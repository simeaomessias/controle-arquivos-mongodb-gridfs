<div class="container-fluid">
    <div class="container px-0 mt-3" style="min-width: 280px; max-width: 600px">
        <button type="button" class="btn btn-info border border-dark" style="width: 140px;" onclick="window.location.href = '/admin/listar-usuarios'">
            <span class="h6">Voltar</span>
        </button>
    </div>
    <div class="mt-3 border border-dark rounded m-auto" style="min-width: 280px; max-width: 600px">
        <h5 class="text-center bg-info border-bottom border-dark rounded-top"><b>INFORMAÇÕES DO USUÁRIO</b></h5>
        <div class="container py-0">
            <form method='POST' action="/admin/editar-usuario/{{usuario._id}}" autocomplete="off">
                <input type="hidden" name='id' value={{usuario._id}}>
                <div class="row mt-1">
                    <div class="col mt-0">
                        <label for="nome" class="form-label h6">Nome</label>
                        <input type="text" class="form-control py-1 border-dark" name="nome" id="input-nome" required="true" autofocus="true" value="{{usuario.nome}}">
                    </div>
                </div>
                <div class="row mt-1">
                    <div class="col mt-0">
                        <label for="nome" class="form-label h6">E-mail</label>
                        <input type="email" class="form-control py-1 border-dark" name="email" id="input-email" required="true" disabled value="{{usuario.email}}">
                    </div>
                </div>
                <div class="row mt-1">
                    <div class="col mt-0">
                        <label for="nome" class="form-label h6">Status</label>
                        <select class="form-select py-1 border-dark" name="status" id="select-status" required="true">
                            <option value="Ativo" selected="true">Ativo</option>
                            <option value="Inativo">Inativo</option>
                        </select>
                    </div>
                </div>
                <script>
                    document.getElementById('select-status').value = "{{usuario.status}}"
                </script>
                <div class="row mt-1">
                    <a href="" data-bs-toggle="modal" data-bs-target="#modalListaDownloads">Downloads feitos pelo usuário</a>
                </div>
                <div class="row mt-2">
                    <div class="col">
                        <button type="button" class="btn disabled btn-info mt-1 mb-3 py-1 col-12">
                            <spanclass="h5">Excluir</span>
                        </button>
                    </div>
                    <div class="col">
                        <button type="submit" class="btn btn-info mt-1 mb-3 py-1 col-12">
                            <spanclass="h5">Salvar</span>
                        </button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>

{{!-- Modal para exibir lista de documentos baixados pelo usuário --}}

<!-- Modal -->
<div class="modal fade" id="modalListaDownloads" tabindex="-1" aria-labelledby="" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content border-bottom border-dark rounded-top">
      <div class="modal-header bg-info py-1 border-bottom border-dark">
        <h6 class="modal-title" id="exampleModalLabel"><b>{{usuario.nome}}</b></h6>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body py-0">
        {{#if listaDownloads}}
            {{#each listaDownloads}}
                <b>{{txtNome}}</b>
                <div>{{txtDescricao}}</div>
            {{/each}}
        {{/if}}
      </div>
      <div class="modal-footer">
        {{!-- <button type="button" class="btn btn-info" data-bs-dismiss="modal">Fechar</button> --}}
        {{!-- <button type="button" class="btn btn-primary">Save changes</button> --}}
      </div>
    </div>
  </div>
</div>


<script src="/global.js"></script>